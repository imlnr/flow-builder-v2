export const NODE_SPACING = {
    HORIZONTAL: 200,
    VERTICAL: 150,
};

export const NODE_DIRECTIONS = {
    TOP: 'top',
    BOTTOM: 'bottom',
    LEFT: 'left',
    RIGHT: 'right',
};

export const EDGE_TYPES = {
    SMOOTH_STEP: 'smoothstep',
    STRAIGHT: 'straight',
    BEZIER: 'default',
};

export const DEFAULT_NODE_STYLE = {
    minWidth: 120,
    minHeight: 60,
};